import { LivestreamScreen } from "@/feature/streaming/pages/livestream-screen";

// Thin Expo Router entry — all logic lives in the streaming feature folder.
export default function LivestreamRoute() {
  return <LivestreamScreen />;
}
